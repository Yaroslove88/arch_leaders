# Архитектурный аудит и миграция к единому источнику истины

**ID плана:** e5af5a72  
**Дата создания:** 2025-01-27  
**Статус:** В процессе выполнения

---

## Резюме проблемы

**Критическая проблема:** Система нарушает принцип единого источника истины (SSOT), смешивая структуру, контент и пользовательские данные в одних и тех же местах хранения.

### Текущие проблемы:

1. **TreeSemantic.data хранит ВСЁ:**
   - Структуру (node_id, branch_id, tier, prerequisites)
   - Контент (name, description) - смешано!
   - Пользовательские данные (state, xp_current) - смешано!

2. **При обновлении seed файла теряются данные:**
   - Перезапись русских переводов английскими из seed
   - Потеря пользовательских данных (state, xp_current)
   - Невозможность безопасно обновлять структуру

3. **Дублирование данных:**
   - Контент хранится в TreeSemantic.data, AbilityNode и node-descriptions.json
   - Нет единого источника истины

---

## Целевая архитектура: Единый источник истины

### Принцип разделения ответственности

**1. Структура (неизменяемая база)**
- **Источник:** `packages/shared/src/seed/initial-ability-tree.json`
- **Содержит:** Только структуру дерева
  - `node_id`, `branch_id`, `tier`, `prerequisites`
  - `unlock_conditions` (структура, не контент)
  - `xp_required` (базовые значения)
- **Хранится в БД:** `TreeSemantic.data` (только структура, без контента)
- **Обновляется:** Только при изменении структуры (добавление/удаление узлов)

**2. Контент (переводы, описания)**
- **Источник:** `data/node-descriptions.json`
- **Содержит:** Только контент
  - `name` (русское название)
  - `full_description`, `practical_meaning`, `examples`
  - `integration_levels`
- **Хранится в БД:** НЕ хранится в `TreeSemantic.data`
- **Обновляется:** Независимо от структуры

**3. Пользовательские данные (уникальны для каждого пользователя)**
- **Источник:** `UserAbilityState` (таблица в БД)
- **Содержит:** Только пользовательские данные
  - `state` (locked, available, active, unlocked, integrated)
  - `xp_current` (текущий прогресс)
  - `progress`, `relevance`
- **Хранится в БД:** Только в `UserAbilityState`
- **Обновляется:** Только через действия пользователя

---

## План миграции

### Этап 1: Аудит и документация ✅ (Завершен)

**Задачи:**
1. ✅ Провести полный аудит архитектуры
   - Документ: `docs/audit/FULL_ARCHITECTURE_AUDIT.md`
   - Выявлены все места хранения, перезаписи и смешивания данных
   
2. ✅ Визуализировать архитектуру
   - Документ: `docs/audit/ARCHITECTURE_VISUALIZATION.md`
   - Диаграммы текущей и правильной архитектуры
   
3. ✅ Создать правила архитектуры
   - Документ: `docs/audit/ARCHITECTURE_RULES.md`
   - Четкие правила разделения ответственности

4. ✅ Документировать правильную архитектуру
   - Документ: `docs/ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md`

### Этап 2: Немедленные исправления ✅ (Завершен)

**Задачи:**
1. ✅ Исправить английские названия в seed файле (6 узлов)
   - `node_shared_leadership`: "Shared Leadership" → "Распределённое лидерство"
   - `node_feedforward`: "Feedforward" → "Обратная связь в будущее"
   - `node_rede_model`: "REDE Model" → "Модель REDE"
   - `node_mirror_holder`: "Window Gazer vs Mirror Holder" → "Смотрящий в окно vs Держащий зеркало"
   - `node_vertical_development`: "Vertical Development" → "Вертикальное развитие"
   - `node_ddo`: "Deliberately Developmental Organization" → "Организация как тренажёр"

2. ✅ Добавить защиту от перезаписи в sync-base-quests.ts
   - Проверка поля `source` перед обновлением квестов
   - Обновляются только квесты с `source='base_template'`
   - Пользовательские квесты защищены от перезаписи

3. ✅ Добавить предупреждение в tree.service.ts
   - Предупреждение о критической проблеме при обновлении seed файла
   - Возможность отключения auto-sync через `DISABLE_TREE_AUTO_SYNC=true`

### Этап 3: Миграция Nodes ⏳ (В процессе)

**Шаг 3.1: Создание скриптов миграции ✅**

1. ✅ `scripts/extract-structure-from-tree.ts`
   - Извлечение структуры (безопасно, не изменяет БД)
   - Создает резервную копию
   - Извлекает только структуру (удаляет name, description, state, xp_current)

2. ✅ `scripts/migrate-tree-separation.ts`
   - Полная миграция структуры и контента
   - Создает резервные копии
   - Извлекает структуру и мигрирует контент
   - Требует флаг `--apply` для применения к БД

3. ✅ `scripts/update-seed-structure-only.ts`
   - Обновление seed файла (только структура)
   - Создает резервную копию
   - Удаляет контент из узлов и веток

**Шаг 3.2: Обновление tree.service.ts ✅**

1. ✅ Добавлен метод `loadNodeContent()` - загрузка контента из node-descriptions.json
2. ✅ Добавлен метод `mergeStructureWithContent()` - объединение структуры и контента в runtime
3. ✅ Обновлен метод `enrichNodeWithContent()` - обогащение узла контентом
4. ✅ Обновлен метод `getSemantic()` - использует разделение структуры и контента
5. ✅ Все пути в `getSemantic()` теперь объединяют структуру и контент в runtime

**Шаг 3.3: Проверка структуры ⏳ (Текущий этап)**

**Команды для выполнения:**
```bash
# 1. Извлечь структуру для проверки (безопасно, не изменяет БД)
npx ts-node scripts/extract-structure-from-tree.ts

# 2. Проверить файл backups/tree-structure-only.json
# Убедиться, что содержит только структуру (нет name, description, state, xp_current)

# 3. Выполнить полную миграцию (создает резервные копии, не изменяет БД без флага)
npx ts-node scripts/migrate-tree-separation.ts

# 4. Проверить файлы в backups/
# - backups/tree-semantic-backup-*.json - резервная копия БД
# - backups/tree-structure-only-migrated.json - структура для проверки
# - backups/node-descriptions-backup-*.json - резервная копия node-descriptions.json
# - data/node-descriptions.json - должен содержать контент для всех узлов
```

**Шаг 3.4: Применение миграции к БД ⏳ (Следующий этап)**

**⚠️ ВНИМАНИЕ:** Перед применением обязательно создайте резервную копию БД!

```bash
# 1. Создать резервную копию БД
pg_dump -U postgres -d leadership_architect > backup_before_migration_$(date +%Y%m%d_%H%M%S).sql

# 2. Применить изменения к БД (транзакция)
npx ts-node scripts/migrate-tree-separation.ts --apply

# 3. Проверить результат в БД
# - TreeSemantic.data должен содержать только структуру
# - Нет полей name, description, state, xp_current в TreeSemantic.data
```

**Шаг 3.5: Обновление seed файла ⏳ (После успешной миграции БД)**

```bash
# 1. Обновить seed файл только структурой
npx ts-node scripts/update-seed-structure-only.ts

# 2. Проверить файл packages/shared/src/seed/initial-ability-tree.json
# Убедиться, что содержит только структуру

# 3. Проверить валидность JSON
npx jsonlint packages/shared/src/seed/initial-ability-tree.json
```

**Шаг 3.6: Тестирование миграции ⏳**

**Проверка структуры:**
- [ ] TreeSemantic.data содержит только структуру (нет name, description, state, xp_current)
- [ ] node-descriptions.json содержит контент для всех узлов
- [ ] seed файл содержит только структуру

**Проверка загрузки данных:**
- [ ] API возвращает дерево с контентом из node-descriptions.json
- [ ] API возвращает дерево с пользовательскими данными из UserAbilityState
- [ ] Контент не перезаписывается при обновлении seed файла

**Проверка обновления:**
- [ ] Обновление seed файла не затрагивает контент
- [ ] Обновление контента не затрагивает структуру
- [ ] Обновление структуры не затрагивает пользовательские данные

### Этап 4: Миграция Quests 📋 (Запланировано)

**Задачи:**
1. Разделить базовые и пользовательские квесты (использовать поле `source`)
2. Обновить `sync-base-quests.ts` для обновления только базовых квестов
3. Защитить пользовательские квесты от перезаписи
4. Создать скрипт миграции для разделения квестов

### Этап 5: Экспорт пользовательских данных 📋 (Запланировано)

**Задачи:**
1. Создать скрипт экспорта пользовательских данных из UserAbilityState
2. Создать скрипт импорта пользовательских данных
3. Добавить валидацию данных при импорте

### Этап 6: Защита от перезаписи 📋 (Запланировано)

**Задачи:**
1. Создать middleware для защиты TreeSemantic от перезаписи
2. Создать guard для проверки source перед обновлением квестов
3. Добавить логирование всех попыток перезаписи
4. Создать автоматические тесты для защиты от перезаписи

---

## Текущий статус

### ✅ Выполнено

1. **Аудит и документация** - полностью завершен
2. **Немедленные исправления** - английские названия исправлены, защита добавлена
3. **Скрипты миграции** - созданы и готовы к использованию
4. **Обновление tree.service.ts** - код обновлен для разделения структуры и контента
5. **Документация миграции** - создана полная документация

### ⏳ В процессе

1. **Проверка структуры** - требуется запустить скрипты и проверить файлы
2. **Применение миграции к БД** - ожидает проверки структуры
3. **Обновление seed файла** - ожидает успешной миграции БД
4. **Тестирование** - ожидает завершения миграции

### 📋 Запланировано

1. Миграция Quests
2. Экспорт пользовательских данных
3. Защита от перезаписи

---

## Критерии успешной миграции

1. ✅ **Структура разделена:**
   - TreeSemantic.data содержит только структуру (node_id, branch_id, tier, prerequisites, unlock_conditions, xp_required)
   - Нет полей name, description, state, xp_current в TreeSemantic.data

2. ✅ **Контент разделен:**
   - Контент хранится в node-descriptions.json
   - Контент не хранится в БД
   - Контент загружается в runtime при запросе

3. ✅ **Пользовательские данные разделены:**
   - Пользовательские данные хранятся только в UserAbilityState
   - Пользовательские данные не хранятся в TreeSemantic.data
   - Пользовательские данные не перезаписываются при обновлении seed файла

4. ✅ **Runtime объединение работает:**
   - API возвращает дерево с контентом из node-descriptions.json
   - API возвращает дерево с пользовательскими данными из UserAbilityState
   - Объединение происходит в runtime, не сохраняется в БД

5. ✅ **Защита от перезаписи:**
   - Обновление seed файла не затрагивает контент
   - Обновление контента не затрагивает структуру
   - Обновление структуры не затрагивает пользовательские данные

---

## Связанные документы

- [FULL_ARCHITECTURE_AUDIT.md](docs/audit/FULL_ARCHITECTURE_AUDIT.md) - Полный аудит архитектуры
- [ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md](docs/ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md) - Правильная архитектура
- [ARCHITECTURE_VISUALIZATION.md](docs/audit/ARCHITECTURE_VISUALIZATION.md) - Визуализация архитектуры
- [ARCHITECTURE_RULES.md](docs/audit/ARCHITECTURE_RULES.md) - Правила архитектуры
- [MIGRATION_GUIDE.md](docs/migration/MIGRATION_GUIDE.md) - Руководство по миграции
- [MIGRATION_PROGRESS.md](docs/migration/MIGRATION_PROGRESS.md) - Прогресс миграции
- [FIX_SOURCE_OF_TRUTH.md](FIX_SOURCE_OF_TRUTH.md) - Исправление источников истины

---

## Примечания

- Этот план был восстановлен на основе существующей документации после потери истории чата
- Все скрипты миграции готовы к использованию
- Перед применением миграции к БД обязательно создайте резервную копию
- Тестирование должно проводиться после каждого этапа миграции

---

**Последнее обновление:** 2025-01-27
