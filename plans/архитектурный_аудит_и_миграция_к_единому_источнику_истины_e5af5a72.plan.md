# Архитектурный аудит и миграция к единому источнику истины

**ID плана:** e5af5a72  
**Дата создания:** 2025-01-27  
**Статус:** Этапы 3, 4, 5 и 6 завершены. Миграция Nodes/Quests/Cases, экспорт/импорт и защита от перезаписи реализованы

---

## Резюме проблемы

**Критическая проблема:** Система нарушает принцип единого источника истины (SSOT), смешивая структуру, контент и пользовательские данные в одних и тех же местах хранения.

### Текущие проблемы:

1. **TreeSemantic.data хранит ВСЁ:**
   - Структуру (node_id, branch_id, tier, prerequisites)
   - Контент (name, description) - смешано!
   - Пользовательские данные (state, xp_current) - смешано!

2. **При обновлении seed файла теряются данные:**
   - Перезапись русских переводов английскими из seed
   - Потеря пользовательских данных (state, xp_current)
   - Невозможность безопасно обновлять структуру

3. **Дублирование данных:**
   - Контент хранится в TreeSemantic.data, AbilityNode и node-descriptions.json
   - Нет единого источника истины

---

## Целевая архитектура: Единый источник истины

### Принцип разделения ответственности

**1. Структура (неизменяемая база)**
- **Источник:** `packages/shared/src/seed/initial-ability-tree.json`
- **Содержит:** Только структуру дерева
  - `node_id`, `branch_id`, `tier`, `prerequisites`
  - `unlock_conditions` (структура, не контент)
  - `xp_required` (базовые значения)
- **Хранится в БД:** `TreeSemantic.data` (только структура, без контента)
- **Обновляется:** Только при изменении структуры (добавление/удаление узлов)

**2. Контент (переводы, описания)**
- **Источник:** `data/node-descriptions.json`
- **Содержит:** Только контент
  - `name` (русское название)
  - `full_description`, `practical_meaning`, `examples`
  - `integration_levels`
- **Хранится в БД:** НЕ хранится в `TreeSemantic.data`
- **Обновляется:** Независимо от структуры

**3. Пользовательские данные (уникальны для каждого пользователя)**
- **Источник:** `UserAbilityState` (таблица в БД)
- **Содержит:** Только пользовательские данные
  - `state` (locked, available, active, unlocked, integrated)
  - `xp_current` (текущий прогресс)
  - `progress`, `relevance`
- **Хранится в БД:** Только в `UserAbilityState`
- **Обновляется:** Только через действия пользователя

---

## План миграции

### Этап 1: Аудит и документация ✅ (Завершен)

**Задачи:**
1. ✅ Провести полный аудит архитектуры
   - Документ: `docs/audit/FULL_ARCHITECTURE_AUDIT.md`
   - Выявлены все места хранения, перезаписи и смешивания данных
   
2. ✅ Визуализировать архитектуру
   - Документ: `docs/audit/ARCHITECTURE_VISUALIZATION.md`
   - Диаграммы текущей и правильной архитектуры
   
3. ✅ Создать правила архитектуры
   - Документ: `docs/audit/ARCHITECTURE_RULES.md`
   - Четкие правила разделения ответственности

4. ✅ Документировать правильную архитектуру
   - Документ: `docs/ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md`

### Этап 2: Немедленные исправления ✅ (Завершен)

**Задачи:**
1. ✅ Исправить английские названия в seed файле (6 узлов)
   - `node_shared_leadership`: "Shared Leadership" → "Распределённое лидерство"
   - `node_feedforward`: "Feedforward" → "Обратная связь в будущее"
   - `node_rede_model`: "REDE Model" → "Модель REDE"
   - `node_mirror_holder`: "Window Gazer vs Mirror Holder" → "Смотрящий в окно vs Держащий зеркало"
   - `node_vertical_development`: "Vertical Development" → "Вертикальное развитие"
   - `node_ddo`: "Deliberately Developmental Organization" → "Организация как тренажёр"

2. ✅ Добавить защиту от перезаписи в sync-base-quests.ts
   - Проверка поля `source` перед обновлением квестов
   - Обновляются только квесты с `source='base_template'`
   - Пользовательские квесты защищены от перезаписи

3. ✅ Добавить предупреждение в tree.service.ts
   - Предупреждение о критической проблеме при обновлении seed файла
   - Возможность отключения auto-sync через `DISABLE_TREE_AUTO_SYNC=true`

### Этап 3: Миграция Nodes, Quests и Cases ✅ (Завершен)

#### 3A. Миграция Nodes (Узлов)

**Шаг 3.1: Создание скриптов миграции Nodes ✅**

1. ✅ `scripts/extract-structure-from-tree.ts`
   - Извлечение структуры (безопасно, не изменяет БД)
   - Создает резервную копию
   - Извлекает только структуру (удаляет name, description, state, xp_current)

2. ✅ `scripts/migrate-tree-separation.ts`
   - Полная миграция структуры и контента
   - Создает резервные копии
   - Извлекает структуру и мигрирует контент
   - Требует флаг `--apply` для применения к БД

3. ✅ `scripts/update-seed-structure-only.ts`
   - Обновление seed файла (только структура)
   - Создает резервную копию
   - Удаляет контент из узлов и веток

**Шаг 3.2: Обновление tree.service.ts ✅**

1. ✅ Добавлен метод `loadNodeContent()` - загрузка контента из node-descriptions.json
2. ✅ Добавлен метод `mergeStructureWithContent()` - объединение структуры и контента в runtime
3. ✅ Обновлен метод `enrichNodeWithContent()` - обогащение узла контентом
4. ✅ Обновлен метод `getSemantic()` - использует разделение структуры и контента
5. ✅ Все пути в `getSemantic()` теперь объединяют структуру и контент в runtime

**Шаг 3.3: Проверка структуры Nodes ✅ (Выполнено)**

**Команды для выполнения:**
```bash
# 1. Извлечь структуру для проверки (безопасно, не изменяет БД)
npx ts-node scripts/extract-structure-from-tree.ts

# 2. Проверить файл backups/tree-structure-only.json
# Убедиться, что содержит только структуру (нет name, description, state, xp_current)

# 3. Выполнить полную миграцию (создает резервные копии, не изменяет БД без флага)
npx ts-node scripts/migrate-tree-separation.ts

# 4. Проверить файлы в backups/
# - backups/tree-semantic-backup-*.json - резервная копия БД
# - backups/tree-structure-only-migrated.json - структура для проверки
# - backups/node-descriptions-backup-*.json - резервная копия node-descriptions.json
# - data/node-descriptions.json - должен содержать контент для всех узлов
```

**Шаг 3.4: Применение миграции к БД ✅ (Выполнено)**

**⚠️ ВНИМАНИЕ:** Резервная копия создана автоматически скриптом!

```bash
# 1. Применить изменения к БД (транзакция) ✅
npx ts-node scripts/migrate-tree-separation.ts --apply

# 2. Проверить результат в БД ✅
# ✅ Проверено через check-migration-result.ts
# - TreeSemantic.data содержит только структуру
# - Нет полей name, description, state, xp_current в TreeSemantic.data
# - Все 40 узлов корректны
```

**Результаты применения:**
- ✅ Резервная копия создана: `backups/tree-semantic-backup-1768063148835.json`
- ✅ TreeSemantic.data обновлен только структурой (40 узлов)
- ✅ Контент мигрирован в node-descriptions.json (0 новых, 40 уже были)
- ✅ Пользовательские данные не затрагивались (остались в UserAbilityState)
- ✅ Транзакция успешно завершена

**Шаг 3.5: Обновление seed файла ✅ (Выполнено)**

```bash
# 1. Обновить seed файл только структурой ✅
npx ts-node scripts/update-seed-structure-only.ts

# 2. Проверить файл packages/shared/src/seed/initial-ability-tree.json ✅
# ✅ Проверено: содержит только структуру (нет name, description, state, xp_current)

# 3. Проверить валидность JSON ✅
# ✅ JSON валиден, структура корректна
```

**Результаты обновления:**
- ✅ Резервная копия создана: `backups/initial-ability-tree-backup-1768063288132.json`
- ✅ Seed файл обновлен только структурой (40 узлов, 6 веток)
- ✅ Структура очищена от контента и пользовательских данных
- ✅ JSON валиден, структура корректна

**Шаг 3.6: Тестирование миграции Nodes ✅ (Выполнено)**

**Проверка структуры:**
- ✅ TreeSemantic.data содержит только структуру (нет name, description, state, xp_current)
- ✅ node-descriptions.json содержит контент для всех 40 узлов
- ✅ seed файл содержит только структуру (обновлен)

**Проверка загрузки данных:**
- ✅ API возвращает дерево с контентом из node-descriptions.json (проверено через тест)
- ✅ API возвращает дерево с пользовательскими данными из UserAbilityState (проверено через тест)
- ✅ Объединение происходит в runtime (структура + контент + пользовательские данные)

**Проверка обновления:**
- ✅ Обновление seed файла не затрагивает контент (контент в node-descriptions.json)
- ✅ Обновление контента не затрагивает структуру (контент в отдельном файле)
- ✅ Обновление структуры не затрагивает пользовательские данные (данные в UserAbilityState)

**Результаты тестирования:**
- ✅ Структура в БД: 40 узлов, все без полей контента и пользовательских данных
- ✅ Контент в node-descriptions.json: 40 узлов с полным контентом
- ✅ Пользовательские данные в UserAbilityState: корректно хранятся и загружаются
- ✅ Runtime объединение: работает правильно (структура + контент + пользовательские данные)

#### 3B. Миграция Quests (Квестов)

**Проблема:**
- Quest таблица хранит базовые квесты (source='base_template'), автоматически сгенерированные (source='auto_generated') и пользовательские (source='user_generated' или null)
- `sync-base-quests.ts` обновляет квесты по `title`, что может перезаписать пользовательские квесты
- ✅ **Исправлено:** `sync-base-quests.ts` уже защищен (обновляет только source='base_template')

**Шаг 3.7: Проверка и валидация Quests ✅ (Выполнено)**

**Архитектура Quests:**
- **Контент (базовые шаблоны):** `data/quest-templates.json` ✅
- **Структура в БД:** Quest таблица с полем `source` ✅
- **Пользовательские данные:** Quest таблица (source='user_generated' или 'auto_generated') ✅

**Команды для выполнения:**
```bash
# 1. Проверить sync-base-quests.ts (уже защищен от перезаписи пользовательских квестов) ✅
# Скрипт: apps/api/src/scripts/sync-base-quests.ts
# ✅ Проверено: обновляет только source='base_template', защищает пользовательские квесты

# 2. Запустить sync-base-quests.ts для проверки (опционально)
# cd apps/api
# npx ts-node src/scripts/sync-base-quests.ts

# 3. Валидация Quests через generate-validation-docs.ts ✅
# ✅ Созданы файлы: 08_QUESTS_STRUCTURE.md, 09_QUESTS_CONTENT.md, 10_QUESTS_FULL.md
# ✅ Статистика: 32 квеста в БД, 33 шаблона в quest-templates.json
# ✅ Разделение: базовые (source='base_template') и пользовательские (source='user_generated'/'auto_generated')
```

**Шаг 3.8: Валидация Quests ✅ (Выполнено)**

**Проверка разделения:**
- ✅ quest-templates.json содержит только базовые шаблоны (контент)
- ✅ Quest таблица правильно разделяет базовые (source='base_template') и пользовательские (source='user_generated'/'auto_generated') квесты
- ✅ sync-base-quests.ts обновляет только базовые квесты (уже исправлено ранее)
- ✅ Пользовательские квесты защищены от перезаписи (защита уже добавлена)

**Документация:**
- ✅ Создан: `08_QUESTS_STRUCTURE.md` - структура квестов из БД
- ✅ Создан: `09_QUESTS_CONTENT.md` - контент из quest-templates.json
- ✅ Создан: `10_QUESTS_FULL.md` - полные данные (структура + контент)

#### 3C. Миграция Cases (Кейсов)

**Архитектура Cases:**
- **Контент:** `data/interactive-cases.json` ✅ (уже правильно разделено)
- **Пользовательские данные:** CaseProgress таблица (прогресс пользователя) ✅ (уже правильно разделено)

**Шаг 3.9: Проверка и валидация Cases ✅ (Выполнено)**

**Текущая архитектура:**
- ✅ Контент хранится в `data/interactive-cases.json` (не в БД)
- ✅ Пользовательские данные (прогресс) хранятся в `CaseProgress` таблице
- ✅ Архитектура уже правильная, требуется только валидация

**Документация:**
- ✅ Создан: `11_CASES_STRUCTURE.md` - структура кейсов
- ✅ Создан: `12_CASES_CONTENT.md` - контент кейсов
- ✅ Создан: `13_CASES_FULL.md` - полные данные кейсов
- ✅ Обновлен: `reports/content-analysis-cases-part1.md` и `part2.md`

**Шаг 3.10: Валидация Cases ✅ (Выполнено)**

**Проверка разделения:**
- ✅ interactive-cases.json содержит только контент (не пользовательские данные)
- ✅ CaseProgress таблица содержит только пользовательские данные (не контент)
- ✅ API правильно загружает контент из JSON и прогресс из БД (проверено через cases.service.ts)
- ✅ Обновление interactive-cases.json не затрагивает пользовательский прогресс (архитектура правильная)

### Этап 4: Финальная валидация и документация ✅ (Завершен)

**Задачи:**
1. ✅ Разделить базовые и пользовательские квесты (использовать поле `source`) - **ВЫПОЛНЕНО**
2. ✅ Обновить `sync-base-quests.ts` для обновления только базовых квестов - **ВЫПОЛНЕНО**
3. ✅ Защитить пользовательские квесты от перезаписи - **ВЫПОЛНЕНО**
4. ✅ Проверить и валидировать разделение квестов - **ВЫПОЛНЕНО** (Шаги 3.7-3.8)
5. ✅ Валидировать Cases - **ВЫПОЛНЕНО** (Шаги 3.9-3.10)
6. ✅ Создать документацию валидации - **ВЫПОЛНЕНО** (14 файлов валидации)

### Этап 5: Экспорт пользовательских данных ✅ (Завершен)

**Задачи:**
1. ✅ Создать скрипт экспорта пользовательских данных из UserAbilityState (`export-user-data.ts`)
   - Экспортирует: UserAbilityState, пользовательские квесты, CaseProgress, пользовательское дерево
   - Опционально: Entry, Session, Evidence (с флагом `--include-content`)
   - Сохраняет в `exports/user-{userId}-{date}.json`
   
2. ✅ Создать скрипт импорта пользовательских данных (`import-user-data.ts`)
   - Импортирует все типы данных из файла экспорта
   - Поддерживает режимы: dry-run (проверка), overwrite (перезапись), обычный (без перезаписи)
   - Использует транзакции для безопасного импорта
   
3. ✅ Добавить валидацию данных при импорте
   - Проверка формата всех типов данных
   - Проверка существования узлов в БД
   - Проверка валидности значений (state, status, dates, numbers)
   - Проверка зависимостей (Session требует Entry)
   - Предупреждения о конфликтах и недостающих данных

**Созданные файлы:**
- ✅ `scripts/export-user-data.ts` - скрипт экспорта пользовательских данных
- ✅ `scripts/import-user-data.ts` - скрипт импорта с валидацией
- ✅ `exports/` - папка для файлов экспорта

**Использование:**
```bash
# Экспорт данных пользователя (без контента)
npx ts-node scripts/export-user-data.ts <userId>

# Экспорт с контентом (Entry, Session, Evidence)
npx ts-node scripts/export-user-data.ts <userId> --include-content

# Импорт с проверкой (dry-run)
npx ts-node scripts/import-user-data.ts <export-file> <target-userId> --dry-run

# Импорт без перезаписи (по умолчанию)
npx ts-node scripts/import-user-data.ts <export-file> <target-userId>

# Импорт с перезаписью существующих данных
npx ts-node scripts/import-user-data.ts <export-file> <target-userId> --overwrite
```

### Этап 6: Защита от перезаписи ✅ (Завершен)

**Задачи:**
1. ✅ Создать guard для защиты TreeSemantic от перезаписи (`ProtectGlobalTreeGuard`)
   - Защищает `tree_main` от изменений обычными пользователями
   - Разрешает изменения только администраторам и системным операциям
   - Владельцы могут изменять свои деревья (`tree_user_*`)
   
2. ✅ Создать guard для проверки source перед обновлением квестов (`ProtectBaseQuestsGuard`)
   - Защищает квесты с `source='base_template'` от перезаписи
   - Разрешает изменения только администраторам и системным операциям
   - Владельцы могут изменять свои квесты (`source='user_generated'` или `'auto_generated'`)
   
3. ✅ Добавить логирование всех попыток перезаписи (`OverwriteProtectionInterceptor`)
   - Логирует попытки изменения защищенных данных
   - Логирует успешные и заблокированные операции
   - Записывает предупреждения о попытках перезаписи
   
4. ✅ Интегрировать guards в контроллеры и модули (выполнено)
   - ✅ Добавлен `ProtectGlobalTreeGuard` в `TreeController` (метод `applyChange`)
   - ✅ Добавлен `ProtectBaseQuestsGuard` в `QuestsController` (методы `update`, `delete`, `activate`, `complete`, `updateStatus`, `updateTheory`)
   - ✅ Добавлен `OverwriteProtectionInterceptor` в оба контроллера
   - ✅ Guards добавлены в providers модулей (`TreeModule`, `QuestsModule`)
   - ✅ Обновлена документация API (добавлены статусы 403 для защищенных endpoints)
   
5. ✅ Создать автоматические тесты для защиты от перезаписи (выполнено)
   - ✅ Создан `protect-global-tree.guard.spec.ts` (7 тестов, все прошли)
   - ✅ Создан `protect-base-quests.guard.spec.ts` (8 тестов, все прошли)
   - ✅ Создан `overwrite-protection.interceptor.spec.ts` (6 тестов, все прошли)
   - ✅ Всего: 21 тест, все прошли успешно

**Созданные файлы:**
- ✅ `apps/api/src/common/guards/protect-global-tree.guard.ts`
- ✅ `apps/api/src/common/guards/protect-base-quests.guard.ts`
- ✅ `apps/api/src/common/interceptors/overwrite-protection.interceptor.ts`
- ✅ `apps/api/src/common/guards/protect-global-tree.guard.spec.ts`
- ✅ `apps/api/src/common/guards/protect-base-quests.guard.spec.ts`
- ✅ `apps/api/src/common/interceptors/overwrite-protection.interceptor.spec.ts`

**Результаты тестирования:**
- ✅ ProtectGlobalTreeGuard: 7/7 тестов прошли
- ✅ ProtectBaseQuestsGuard: 8/8 тестов прошли
- ✅ OverwriteProtectionInterceptor: 6/6 тестов прошли
- ✅ Всего: 21 тест успешно пройден

---

## Текущий статус

### ✅ Выполнено

1. **Аудит и документация** - полностью завершен
2. **Немедленные исправления** - английские названия исправлены, защита добавлена
3. **Скрипты миграции Nodes** - созданы и готовы к использованию
4. **Обновление tree.service.ts** - код обновлен для разделения структуры и контента
5. **Документация миграции** - создана полная документация
6. **Проверка структуры Nodes** - скрипты выполнены, структура извлечена корректно
7. **Валидация Quests и Cases** - скрипт generate-validation-docs.ts исправлен и выполнен
8. **Документация Quests и Cases** - созданы файлы валидации (08-13_*.md)
9. **Применение миграции к БД** - успешно применено, структура разделена
10. **Обновление seed файла** - seed файл обновлен только структурой
11. **Тестирование миграции** - все проверки пройдены, миграция успешна

### ⏳ В процессе

1. **Тестирование в продакшене** - проверить работу API с реальными пользователями
2. **Мониторинг** - убедиться, что нет регрессий после миграции и защиты

### 📋 Запланировано

1. ~~Миграция и валидация Quests~~ ✅ Выполнено (Шаги 3.7-3.8)
2. ~~Валидация Cases~~ ✅ Выполнено (Шаги 3.9-3.10)
3. ~~**Экспорт пользовательских данных**~~ ✅ Выполнено (Этап 5) - скрипты экспорта/импорта созданы с валидацией
4. ~~**Защита от перезаписи**~~ ✅ Выполнено (Этап 6) - guards, interceptors и тесты созданы и протестированы
5. **Тестирование в продакшене** - проверить работу API с реальными пользователями

---

## Критерии успешной миграции

1. ✅ **Структура разделена:**
   - TreeSemantic.data содержит только структуру (node_id, branch_id, tier, prerequisites, unlock_conditions, xp_required)
   - Нет полей name, description, state, xp_current в TreeSemantic.data

2. ✅ **Контент разделен:**
   - Контент хранится в node-descriptions.json
   - Контент не хранится в БД
   - Контент загружается в runtime при запросе

3. ✅ **Пользовательские данные разделены:**
   - Пользовательские данные хранятся только в UserAbilityState
   - Пользовательские данные не хранятся в TreeSemantic.data
   - Пользовательские данные не перезаписываются при обновлении seed файла

4. ✅ **Runtime объединение работает:**
   - API возвращает дерево с контентом из node-descriptions.json
   - API возвращает дерево с пользовательскими данными из UserAbilityState
   - Объединение происходит в runtime, не сохраняется в БД

5. ✅ **Защита от перезаписи:**
   - Обновление seed файла не затрагивает контент
   - Обновление контента не затрагивает структуру
   - Обновление структуры не затрагивает пользовательские данные

---

## Связанные документы

- [FULL_ARCHITECTURE_AUDIT.md](../docs/audit/FULL_ARCHITECTURE_AUDIT.md) - Полный аудит архитектуры
- [ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md](../docs/ARCHITECTURE_SINGLE_SOURCE_OF_TRUTH.md) - Правильная архитектура
- [ARCHITECTURE_VISUALIZATION.md](../docs/audit/ARCHITECTURE_VISUALIZATION.md) - Визуализация архитектуры
- [ARCHITECTURE_RULES.md](../docs/audit/ARCHITECTURE_RULES.md) - Правила архитектуры
- [MIGRATION_GUIDE.md](../docs/migration/MIGRATION_GUIDE.md) - Руководство по миграции
- [MIGRATION_PROGRESS.md](../docs/migration/MIGRATION_PROGRESS.md) - Прогресс миграции
- [FIX_SOURCE_OF_TRUTH.md](../FIX_SOURCE_OF_TRUTH.md) - Исправление источников истины

---

## Примечания

- Этот план был восстановлен на основе существующей документации после потери истории чата
- Все скрипты миграции готовы к использованию
- Перед применением миграции к БД обязательно создайте резервную копию
- Тестирование должно проводиться после каждого этапа миграции

**Обновления 2026-01-10:**
- ✅ Добавлена миграция и валидация Quests в Этап 3 (3B)
- ✅ Добавлена миграция и валидация Cases в Этап 3 (3C)
- ✅ Исправлен скрипт generate-validation-docs.ts для поддержки Quests и Cases
- ✅ Созданы файлы валидации: 08-13_*.md (Quests и Cases)
- ✅ Обновлены файлы в reports/ с данными Quests и Cases
- ✅ Quests: архитектура правильная (защита от перезаписи уже добавлена в sync-base-quests.ts)
- ✅ Cases: архитектура правильная (контент в JSON, прогресс в БД)
- ✅ Миграция Nodes применена к БД (структура разделена)
- ✅ Seed файл обновлен только структурой
- ✅ Тестирование миграции завершено успешно
- ✅ Все критерии успешной миграции выполнены
- ✅ Этап 5 (Экспорт пользовательских данных) завершен:
  - ✅ Создан скрипт экспорта (`export-user-data.ts`)
  - ✅ Создан скрипт импорта с валидацией (`import-user-data.ts`)
  - ✅ Добавлена полная валидация всех типов данных
  - ✅ Поддержка режимов: dry-run, overwrite, обычный
- ✅ Этап 6 (Защита от перезаписи) завершен:
  - ✅ Созданы guards для защиты TreeSemantic и базовых квестов
  - ✅ Создан interceptor для логирования попыток перезаписи
  - ✅ Guards интегрированы в контроллеры и модули
  - ✅ Добавлена защита на уровне сервиса для методов массового обновления
  - ✅ Созданы и пройдены автоматические тесты (21 тест, все прошли)

---

**Последнее обновление:** 2026-01-10  
**Изменения:** 
- Добавлена миграция и валидация Quests и Cases в Этап 3
- Этап 3 завершен: миграция Nodes, Quests и Cases выполнена успешно
- Этап 5 завершен: экспорт/импорт пользовательских данных реализован с валидацией
- Этап 6 завершен: защита от перезаписи реализована и протестирована
- Все тесты пройдены (21 тест для защиты от перезаписи), система работает корректно
