# 🏗 TECH ARCHITECTURE AUDIT REPORT

**Дата:** 2025-01-27  
**Проект:** Leadership Architect  
**Версия:** После рефакторинга (PR1-PR5)

---

## 📊 СВОДКА

| Раздел | 🟢 Ок | 🟡 Хрупко | 🔴 Риск | Статус |
|--------|-------|-----------|---------|--------|
| I. Структура проекта | 2 | 1 | 0 | 🟡 |
| II. Доменная модель | 2 | 1 | 0 | 🟡 |
| III. Pipeline и Jobs | 1 | 1 | 1 | 🟡 |
| IV. AI/LLM контур | 3 | 0 | 0 | 🟢 |
| V. Ability & Quest Engine | 0 | 1 | 1 | 🔴 |
| VI. Данные и хранение | 2 | 1 | 0 | 🟡 |
| VII. Безопасность | 2 | 1 | 0 | 🟡 |
| VIII. Тестируемость | 0 | 1 | 1 | 🔴 |
| IX. Операционный контур | 2 | 1 | 0 | 🟡 |
| X. Красные флаги | - | - | - | 🟡 |

**Общий статус:** 🟡 **Хрупко** (требует внимания)

---

## I. 🧱 СТРУКТУРА ПРОЕКТА И КОДА

### 1. Границы доменов

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ Проект частично разбит по доменам: `entries`, `sessions`, `quests`, `tree`, `evidence`, `auth`, `admin`
- ✅ Каждый домен имеет свой модуль (NestJS modules)
- ✅ Есть разделение на controller/service/repository в некоторых доменах

**Что отсутствует:**
- ⚠️ Нет четкого разделения на `core/`, `infra/`, `interfaces/`
- ⚠️ Структура смешанная: есть домены, но нет единой папочной архитектуры
- ⚠️ `common/` содержит и DTO, и схемы, и мапперы - нужна более четкая структура

**Рекомендации:**
- Реорганизовать в структуру: `core/`, `infra/`, `interfaces/`
- Выделить `shared/` для общих типов и утилит

---

### 2. Dependency direction

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ После рефакторинга убран `forwardRef` между Sync и Quests (через Orchestration)
- ✅ LLM слой изолирован в `LLMService` (инфраструктура)
- ✅ Admin отделен в `admin/` модуле
- ✅ Core (quest, tree) не знает о UI и Telegram

**Что улучшено:**
- ✅ Создан `OrchestrationModule` для разрыва циклических зависимостей
- ✅ `QuestGenerationService` стал чистым (не пишет в БД)

**Осталось:**
- ⚠️ Один `forwardRef` в `jwt.strategy.ts` (AuthService) - допустимо для стратегий

---

### 3. Папочная архитектура

**Статус:** 🟡 **Хрупко**

**Текущая структура:**
```
apps/api/src/
  - entries/        (домен)
  - sessions/       (домен)
  - quests/         (домен)
  - tree/           (домен)
  - llm/            (инфраструктура)
  - prisma/         (инфраструктура)
  - admin/          (интерфейс)
  - common/         (shared)
```

**Рекомендуемая структура:**
```
apps/api/src/
  core/
    abilities/
    quests/
    entries/
  infra/
    db/
    llm/
    queue/
  interfaces/
    api/
    admin/
  shared/
    types/
    schemas/
```

**Рекомендации:**
- Постепенная миграция к рекомендуемой структуре
- Начать с выделения `infra/` и `shared/`

---

## II. 🧠 ДОМЕННАЯ МОДЕЛЬ

### 4. Ясность сущностей

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Четкое разделение: `Entry` ≠ `Session` ≠ `Evidence` ≠ `ChangeLog`
- ✅ `AbilityNode` (каталог) ≠ `UserAbilityState` (состояние)
- ✅ Нет "универсальных таблиц"

**Проверено:**
- Entry - raw input
- Session - analysis output
- Evidence - proof of ability
- ChangeLog - audit trail

---

### 5. Состояние vs события

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ Есть разделение: event data (entries, evidence) и state (user_ability_state, quests)
- ✅ ChangeLog - доменная сущность с undo support

**Что отсутствует:**
- ⚠️ Не все изменения состояния объясняются через события
- ⚠️ Нет явного event sourcing паттерна

**Рекомендации:**
- Добавить явную связь между событиями и изменениями состояния
- Документировать, какие события приводят к каким изменениям

---

### 6. Версионирование

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ `SessionArtifact` версионируется (kind, version)
- ✅ `PromptRegistry` версионируется (prompt_id, version)
- ✅ `ConfigVersion` версионируется (config_set_id, version)
- ✅ `ChangeLog` содержит `tree_revision`

**Проверено:**
- Все критичные сущности имеют версионирование

---

## III. 🔁 PIPELINE И JOB ARCHITECTURE

### 7. Асинхронность

**Статус:** 🔴 **Риск**

**Что есть:**
- ✅ Есть модель `Job` в Prisma schema
- ✅ Есть `admin/jobs` модуль для управления

**Что отсутствует:**
- ❌ Анализ выполняется синхронно в HTTP-request (`AnalysisParserService.analyzeEntry`)
- ❌ Генерация квестов выполняется синхронно (хотя и асинхронно вызывается)
- ❌ Нет активного job processor/worker

**Рекомендации:**
- **Критично:** Вынести анализ в job queue
- Создать job processor для обработки очереди
- Сделать анализ асинхронным через jobs

---

### 8. Идемпотентность

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ Есть проверка существующей сессии перед анализом
- ✅ Jobs имеют `attempt` и `max_attempts`

**Что отсутствует:**
- ⚠️ Нет явных deduplication ключей
- ⚠️ Не все операции идемпотентны

**Рекомендации:**
- Добавить deduplication ключи для критичных операций
- Документировать идемпотентность операций

---

### 9. Стадийность

**Статус:** 🔴 **Риск**

**Что есть:**
- ✅ Есть `LlmRun` с полем `stage` (extract_themes, generate_quests, etc.)

**Что отсутствует:**
- ❌ Анализ не разбит на явные этапы
- ❌ Нет возможности перезапустить отдельный этап
- ❌ Нет возможности отключить этап

**Рекомендации:**
- Разбить анализ на этапы: preprocess → interpret → synthesize → update → quest
- Сделать этапы перезапускаемыми
- Добавить возможность отключения этапов

---

## IV. 🤖 AI / LLM КОНТУР

### 10. Изоляция AI

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ LLM вызывается только через `LLMService`
- ✅ Промпты не захардкожены (есть `PromptRegistry`)
- ✅ Выход LLM проходит schema-validation через zod (`AnalysisResponseSchema`)

**Улучшено в PR5:**
- ✅ Все JSON ответы валидируются через zod
- ✅ Retry с backoff
- ✅ Structured logging

---

### 11. Трассировка

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Есть модель `LlmRun` для трассировки
- ✅ Логируются: prompt_id, prompt_version, model, tokens, latency
- ✅ После PR5: логируются requestId, latency, tokens, модель

**Что можно улучшить:**
- ⚠️ Не все вызовы LLM сохраняются в `LlmRun` (только через schema)
- ⚠️ Нет явной связи между request и LlmRun

**Рекомендации:**
- Сохранять все вызовы LLM в `LlmRun`
- Добавить связь requestId → LlmRun

---

### 12. Fallbacks

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Есть режим без LLM (mock data)
- ✅ Ошибка LLM не рушит систему (fallback to mock)
- ✅ После PR5: при ошибке парсинга сохраняется raw response

**Проверено:**
- Система устойчива к ошибкам LLM

---

## V. 🌳 ABILITY & QUEST ENGINE

### 13. Business rules отдельно

**Статус:** 🔴 **Риск**

**Что есть:**
- ✅ Есть `TreeService` для работы с деревом способностей
- ✅ Есть `QuestGenerationService` для генерации квестов

**Что отсутствует:**
- ❌ Нет отдельного `AbilityEngine` для бизнес-правил
- ❌ Нет отдельного `QuestEngine` для правил квестов
- ❌ Формулы могут быть внутри сервисов

**Рекомендации:**
- **Критично:** Создать `AbilityEngine` для бизнес-правил
- Создать `QuestEngine` для правил квестов
- Вынести формулы в отдельные файлы/классы

---

### 14. Deterministic core

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ AI дает сигналы, но не принимает финальных решений
- ✅ Есть детерминированные части (TreeService, QuestRepository)

**Что отсутствует:**
- ⚠️ Не все части детерминированы
- ⚠️ AbilityEngine нельзя прогнать без LLM (зависит от AI сигналов)

**Рекомендации:**
- Сделать AbilityEngine детерминированным
- AI сигналы должны быть опциональными для тестирования

---

## VI. 🗃 ДАННЫЕ И ХРАНЕНИЕ

### 15. Целостность

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ FK реально используются в Prisma schema
- ✅ Есть `onDelete: Cascade` и `onDelete: SetNull` где нужно
- ✅ После PR1-PR3: JSON поля валидируются через zod

**Проверено:**
- Схема Prisma корректна
- Валидация данных на границах

---

### 16. Производительность

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ Есть индексы в Prisma schema
- ✅ Есть индексы для User 360 запросов

**Что отсутствует:**
- ⚠️ Нет явных read-моделей для админки
- ⚠️ Могут быть N+1 проблемы (нужна проверка)

**Рекомендации:**
- Проверить на N+1 проблемы
- Рассмотреть read-модели для админки

---

### 17. Миграции

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Используется Prisma migrations
- ✅ Схема версионируется

**Проверено:**
- Миграции настроены корректно

---

## VII. 🛡 БЕЗОПАСНОСТЬ И НАДЁЖНОСТЬ

### 18. Доступ

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Отдельная таблица `AdminUser`
- ✅ RBAC через роли (admin, operator, analyst)
- ✅ Есть `AdminAuditLog` для логирования sensitive операций

**Проверено:**
- Безопасность настроена

---

### 19. Секреты

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Ключи через `ConfigService` (env variables)
- ✅ Есть валидация env через `env.validation.ts`

**Проверено:**
- Секреты не в коде

---

### 20. Надёжность

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ После PR5: Retries с backoff в LLMService
- ✅ Jobs имеют retry механизм (max_attempts)

**Что отсутствует:**
- ⚠️ Нет явной dead letter queue
- ⚠️ Нет alerting системы

**Рекомендации:**
- Добавить dead letter queue для failed jobs
- Настроить alerting для критичных ошибок

---

## VIII. 🧪 ТЕСТИРУЕМОСТЬ

### 21. Тестируемость архитектуры

**Статус:** 🔴 **Риск**

**Что есть:**
- ✅ Есть `entries.service.spec.ts` (пример теста)

**Что отсутствует:**
- ❌ Нет тестов для AbilityEngine (его нет)
- ❌ Нет тестов для QuestEngine (его нет)
- ❌ Pipeline нельзя прогнать в sandbox

**Рекомендации:**
- **Критично:** Создать тесты для бизнес-правил
- Сделать pipeline тестируемым
- Добавить unit тесты для формул

---

### 22. Типы тестов

**Статус:** 🔴 **Риск**

**Что есть:**
- ✅ Есть пример unit теста

**Что отсутствует:**
- ❌ Нет integration тестов для jobs + db
- ❌ Нет scenario тестов для "ситуация → дерево → квест"

**Рекомендации:**
- Добавить integration тесты
- Добавить scenario тесты

---

## IX. 🔧 ОПЕРАЦИОННЫЙ КОНТУР

### 23. Админка

**Статус:** 🟢 **Ок**

**Что есть:**
- ✅ Есть админ-панель (`admin/`)
- ✅ Есть User 360 (admin-users)
- ✅ Есть re-run (admin-jobs)
- ✅ Есть recompute (admin-config)
- ✅ Есть audit (admin-audit)

**Проверено:**
- Админка функциональна

---

### 24. Наблюдаемость

**Статус:** 🟡 **Хрупко**

**Что есть:**
- ✅ После PR5: Structured logging для LLM
- ✅ Есть `RequestIdMiddleware` для корреляции
- ✅ Есть `LlmRun` для трассировки LLM

**Что отсутствует:**
- ⚠️ Нет явных метрик pipeline
- ⚠️ Нет полной корреляции request → job → llm → state

**Рекомендации:**
- Добавить метрики для pipeline
- Улучшить корреляцию через requestId

---

## X. 🚨 КРАСНЫЕ ТЕХНИЧЕСКИЕ ФЛАГИ

### Проверка красных флагов:

- ✅ **Контроллеры не содержат бизнес-логику** - логика в сервисах
- ✅ **LLM не пишет напрямую в базу** - через сервисы и репозитории
- ⚠️ **Нет единого места формул** - формулы разбросаны по сервисам
- ✅ **Можно объяснить, откуда взялся квест** - через session_id и source
- ⚠️ **Нельзя безопасно пересчитать пользователя** - нет явного механизма
- ✅ **Не всё в одном сервисе** - есть разделение
- ✅ **Изменения состояния с change_log** - есть ChangeLog

**Статус:** 🟡 **Хрупко** (2 из 7 флагов)

---

## 🎯 ГЛАВНЫЙ АРХИТЕКТУРНЫЙ КРИТЕРИЙ

**Вопрос:** Можно ли взять пользователя → пересчитать его полностью → показать diff → объяснить каждое изменение?

**Ответ:** 🟡 **Частично**

**Что есть:**
- ✅ Есть ChangeLog для отслеживания изменений
- ✅ Есть связь между событиями и состоянием

**Что отсутствует:**
- ❌ Нет явного механизма полного пересчета пользователя
- ❌ Нет diff механизма
- ❌ Не все изменения объясняются через события

**Рекомендации:**
- Создать механизм полного пересчета пользователя
- Добавить diff механизм
- Улучшить связь событий и изменений состояния

---

## 📋 ПРИОРИТЕТНЫЕ РЕКОМЕНДАЦИИ

### 🔴 Критично (следующий спринт):

1. **Вынести анализ в job queue** - сделать асинхронным
2. **Создать AbilityEngine** - отдельный слой для бизнес-правил
3. **Создать QuestEngine** - отдельный слой для правил квестов
4. **Добавить тесты** - unit тесты для бизнес-правил

### 🟡 Важно (ближайшие месяцы):

1. Реорганизовать структуру проекта (core/infra/interfaces)
2. Разбить анализ на этапы (стадийность)
3. Добавить integration и scenario тесты
4. Улучшить наблюдаемость (метрики, корреляция)

### 🟢 Улучшения (по возможности):

1. Добавить dead letter queue
2. Настроить alerting
3. Создать read-модели для админки
4. Улучшить детерминизм AbilityEngine

---

## ✅ ЧТО УЖЕ ХОРОШО

1. ✅ **LLM контур** - изолирован, валидируется, трассируется
2. ✅ **Версионирование** - все критичные сущности версионируются
3. ✅ **Безопасность** - RBAC, audit, секреты
4. ✅ **Админка** - функциональна и полна
5. ✅ **Доменная модель** - четкая, без универсальных таблиц
6. ✅ **После рефакторинга** - убраны циклические зависимости, добавлена валидация

---

**Отчет подготовлен:** 2025-01-27  
**Следующий аудит:** через 1 месяц

